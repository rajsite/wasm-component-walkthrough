# Hono Wasi Key-Value App

Requires:
- node
- wasmtime
- ab (optional for benchmarking)
    - linux: `sudo apt install apache2-utils`

Usage:
- `npm install`
- `npm run build`

Run with:
- `npm run start`
